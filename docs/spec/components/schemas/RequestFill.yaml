allOf:
  - $ref: '#/components/schemas/RequestFillKey'
  - type: object
    x-go-is-request: true
    required:
      - attributes
    properties:
      attributes:
        required:
          - credential_requests
        type: object
        properties:
          credential_requests:
            type: array
            description: Credential requests to an issuer
            items:
              $ref: '#/components/schemas/CredentialRequest'
